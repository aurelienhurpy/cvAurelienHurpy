<nav class="navbar navbar-expand-lg navbar-light header_admin">

    <div class="col text-center mt-5">
   
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
    <div class="collapse navbar-collapse mt-5" id="navbarColor01">
         <div class="googleleftheader">
            <a class="nav-link board" href="{{ path('admin_dashboard') }}"><h1 class="googletitle2"><span class="darkgoogleind"></span> <span class="bluegoogleind">H</span><span class="bluegoogle2ind">i</span><span class="bluegoogle2ind">r</span><span class="bluegoogle2ind">e </span><span class="greengoogleind">M</span><span class="greengoogleind2">e!</span></h1></a>
        </div>
        <ul class="navbar-nav">
            <li class="nav-item ">
                <a class="nav-link board" href="{{ path('admin_dashboard') }}"><i class="fas fa-chart-line"></i> Tableau de bord</a>
            </li>
            <li class="nav-item boar">
                <a href="{{ path('homepage') }}" class="nav-link board boardb" target="_blank"><i class="fas fa-eye"></i> Voir le site</a>
            </li>
        </ul>
        <ul class="navbar-nav ">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown">
                        
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a class="dropdown-item" href="{{ path('admin_account_home') }}">Profil</a>
                        <a class="dropdown-item" href="{{ path('admin_account_login') }}">Deconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

        